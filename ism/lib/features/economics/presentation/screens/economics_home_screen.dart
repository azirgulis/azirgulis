import 'package:flutter/material.dart';

class EconomicsHomeScreen extends StatelessWidget {
  const EconomicsHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Economics Tower')),
      body: const Center(child: Text('Economics - Coming Soon')),
    );
  }
}
